
public readonly struct AutoTile
{
    public readonly int BitmapIndex;
    public readonly string Path;

    public AutoTile(int bitmapIndex, string path)
    {
        BitmapIndex = bitmapIndex;
        Path = path;
    }
}