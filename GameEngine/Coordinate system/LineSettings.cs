
public class LineSettings
{
    public readonly float Width;
    public readonly float Length;

    public LineSettings(float width, float length)
    {
        Width = width;
        Length = length;
    }
}